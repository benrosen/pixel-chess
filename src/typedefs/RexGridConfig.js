import RexGridPerspective from "./RexGridPerspective";
import RexGridType from "./RexGridType";

/**
 * The config object for a board grid.
 *
 * @typedef RexGridConfig
 * @property {RexGridType} gridType The type of grid.
 * @property {number} x The number of columns.
 * @property {number} y The number of rows.
 * @property {number} cellWidth The width of each cell.
 * @property {number} cellHeight The height of each cell.
 * @property {RexGridPerspective} type The type of perspective.
 * @see {module:boardConfig}
 */
