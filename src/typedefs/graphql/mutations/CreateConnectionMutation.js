/**
 * Create a connection to a given game.
 *
 * @callback CreateConnectionMutation
 * @param {CreateConnectionInput} input The input data.
 * @returns {Connection} The newly created Connection.
 */
